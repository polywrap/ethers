#import { Module, Connection } into EthereumProvider from "wrap://ens/interface.ethereum-provider.polywrap.eth"

type Env {
  connection: EthereumProvider_Connection
}

type Module implements EthereumProvider {}
